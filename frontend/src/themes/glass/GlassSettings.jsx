/**
 * GlassSettings.jsx - Glass Theme Settings Page
 *
 * Wraps the standard Settings component. The glass CSS overrides
 * handle visual transformation.
 */
import SettingsPage from '../../pages/Settings'

export default function GlassSettings() {
  return <SettingsPage />
}
