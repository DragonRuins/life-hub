/**
 * GlassNotes.jsx - Glass Theme Notes Page
 *
 * Wraps the standard Notes component. The glass CSS overrides
 * handle visual transformation of .card, .btn, input elements.
 */
import Notes from '../../pages/notes/Notes'

export default function GlassNotes() {
  return <Notes />
}
