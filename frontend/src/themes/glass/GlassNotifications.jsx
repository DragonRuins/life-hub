/**
 * GlassNotifications.jsx - Glass Theme Notification Settings
 *
 * Wraps the standard Notifications component. The glass CSS overrides
 * handle visual transformation of tab navigation and panels.
 */
import Notifications from '../../pages/Notifications'

export default function GlassNotifications() {
  return <Notifications />
}
