/**
 * GlassProjects.jsx - Glass Theme Projects Page
 *
 * Wraps the standard Projects component. Glass CSS overrides
 * handle visual transformation of .card, .btn, input elements.
 */
import Projects from '../../pages/Projects'

export default function GlassProjects() {
  return <Projects />
}
